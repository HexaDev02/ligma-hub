━━━━━━━━━━━━━━━━━━━━━━━ ⚫ END OF LIGMA HUB ⚫ ━━━━━━━━━━━━━━━━━━━━━━━
    "Ligma Hub" is now officially **discontinued**.

    Created in collaboration with **Hexa Hub**, the project has
    been retired and will **most likely never return**.

    ✘ No further updates  
    ✘ No bug fixes  
    ✘ No future versions

    Thank you for the support — the legacy lives on.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    ⚠️ Status: This script is no longer operational.
               Archived for nostalgia and documentation.

    – HexaDev.01
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

    -- [ System.LIGMA.Core.Internal.Config ]
    -- { Do not touch unless you know what you're doing }

    local function _initializeCore()
        -- Placeholder
        -- return 42 -- ?
    end

    local function _loadObsoleteModule()
        -- This module was deprecated in version 1.2.0
        -- It's kept here... for reasons.
    end

    local function _activate()
        -- You weren't supposed to find this.
    end

    -- End of line.

    local function _permdev()
    local function _value(10) 
    local username _dev
    if dev local function get value number () charge number xy-tt() 
    
    end
    -- admin zone.
